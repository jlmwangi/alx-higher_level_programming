const header = document.querySelector('header');

function updateColor() {
	//declare a variable to hold the color
	const red = '#FF0000';
	// update text color of header element
	header.style.color = red;
}
updateColor();
